{
  "name": "paths-untold",
  "version": "0.1.2",
  "private": true,
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "scripts": {
    "start": "vite --configLoader native",
    "build": "vite build --configLoader native",
    "preview": "vite preview --configLoader native",
    "test": "vitest run --configLoader native",
    "test:watch": "vitest --configLoader native",
    "dev": "concurrently -k -n SERVER,WEB -c yellow,cyan \"npm --prefix server run dev\" \"vite --configLoader native\""
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@vitejs/plugin-react": "^6.0.1",
    "autoprefixer": "^10.4.21",
    "concurrently": "^9.2.1",
    "jsdom": "^29.0.1",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "vite": "^8.0.2",
    "vitest": "^4.1.1"
  }
}
